# 외부 AI 교차 검토 설정

유피스의 기본 대화와 팀 의견은 계속 로컬 Qwen으로 실행됩니다. 이 문서는 사용자가 버튼을
눌렀을 때만 Gemini와 Claude를 추가로 호출하는 `외부 AI 교차 검토` 설정 방법입니다.

## 역할

- Gemini: 대화에서 확정된 것·제안·미확인 조건을 JSON으로 정리합니다.
- Claude: Gemini 정리와 프로젝트 대화를 독립적으로 검토해 빠진 조건과 위험을 찾습니다.
- 유키: 두 결과를 채팅의 한 검토 기록으로 보여 줍니다.

두 서비스 모두 API 키와 모델명을 설정해야 버튼이 활성화됩니다. 키가 없는 상태에서는
외부 요청을 보내지 않으므로 비용이 발생하지 않습니다.

## Windows 환경 변수 설정

각 서비스의 공식 콘솔에서 API 키를 만든 뒤, PowerShell에 아래 형식으로 입력합니다.
`<...>` 부분에는 실제 값만 넣고, 키를 채팅·코드·프로젝트 파일에 저장하지 않습니다.

```powershell
setx GEMINI_API_KEY "<Gemini API 키>"
setx YOUFFICE_GEMINI_MODEL "<Gemini에서 사용할 모델 ID>"
setx ANTHROPIC_API_KEY "<Claude API 키>"
setx YOUFFICE_CLAUDE_MODEL "<Claude에서 사용할 모델 ID>"
```

설정 후에는 열린 터미널과 유피스 서버를 모두 종료한 뒤 다시 실행해야 새 환경 변수가
읽힙니다. 모델 ID는 가입한 서비스 콘솔에서 현재 사용 가능한 값을 확인해 넣습니다.

## 사용 방법

1. 유키와 프로젝트를 어느 정도 대화로 정리합니다.
2. 대화 하단의 `✨ 외부 AI 교차 검토`를 누릅니다.
3. Gemini 조건 정리와 Claude 독립 검토가 완료되면 유키 채팅에 저장됩니다.
4. 검토 내용을 보고 계속 대화하거나, `팀 의견 받기`, `팀에 맡기기`로 이어갑니다.

외부 AI의 결과도 사실로 자동 확정되지 않습니다. 가격·재고·구매 상품·호환성은 기존의
웹 자료 찾기와 디바이스마트 부품 찾기로 근거를 확인한 뒤 결정합니다.
