# YOUFFICE 작업 지침

작업을 시작하기 전에 `AI_WORK_RULES.md`를 반드시 읽는다.

## 절대 규칙

- 여러 AI가 같은 파일을 동시에 수정하지 않는다.
- 담당 폴더 밖의 파일은 임의로 수정하지 않는다.
- 한 번에 기능 하나만 변경한다.
- 수정 후 문법 검사 → import 검사 → 앱 실행 검사를 진행한다.
- `app.py`, `database.py`, `requirements.txt`는 통합 핵심 파일이다.
- 사용자가 요청한 기능을 구현하기 위해 핵심 파일 변경이 필요하면 변경 이유와
  범위를 작업 중간 안내로 먼저 알린 뒤, 별도 승인 질문 없이 수정·검증한다.
- 요청 범위를 벗어나는 기능 확대, 데이터 삭제·초기화, 이전 프로젝트나 성공
  백업 변경, 외부 비용·계정 권한이 필요한 작업은 별도 확인을 받는다.
- 성공 백업 `Youffice_stage5_success_backup_20260807_0222`는 수정하지 않는다.

## 담당 영역

- `conversation/`: 프롬프트와 응답 품질
- `ui/`: 화면과 UI 코드
- `workflow/`: 업무 배정·회의·검수·보고 흐름
- `animation/`: 애니메이션 코드와 리소스
- `tools/`: 점검·백업·복구 도구

현재 작업과 관계없는 대규모 리팩터링은 수행하지 않는다.
