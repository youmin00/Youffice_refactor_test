# YOUFFICE AI 작업 분담 규칙

## 공통 원칙

1. 두 개 이상의 AI가 같은 파일을 동시에 수정하지 않는다.
2. 한 번에 기능 하나만 수정하고 바로 테스트한다.
3. 작업 전 반드시 백업을 확인한다.
4. 사용자가 요청한 기능을 구현하기 위해 담당 범위 밖의 파일 수정이 필요하면
   이유와 범위를 작업 중간 안내로 알린 뒤 별도 승인 질문 없이 진행한다.
5. app.py와 database.py는 통합 핵심 파일이므로 요청과 관계없이 임의 수정하지
   않는다. 사용자가 요청한 기능에 꼭 필요한 변경은 이유와 범위를 알린 뒤 바로
   수정·검증한다.
6. 수정 후 문법 검사 → import 검사 → 실제 앱 검사를 순서대로 진행한다.
7. 정상 작동이 확인되기 전에는 다음 기능으로 넘어가지 않는다.

## 사용자 요청 즉시 실행 원칙

1. 사용자가 구현·수정·적용·오류 해결을 명확히 요청했다면 해당 요청 자체를 작업
   승인으로 간주한다. 같은 변경에 대해 다시 승인 여부를 묻지 않는다.
2. 먼저 현재 코드와 관련 규칙을 조사한 뒤 필요한 파일을 직접 수정하고, 문법·
   import·관련 회귀·Streamlit AppTest·임시 포트 실행·preflight를 가능한 범위에서
   수행한다.
3. app.py, database.py, requirements.txt, 패키지 __init__.py 또는 담당 영역 밖 파일이
   필요해도 요청 범위 안이라면 변경 이유와 범위를 작업 중간 안내로 알리고 계속
   진행한다.
4. 다음 경우에만 작업을 멈추고 사용자에게 확인한다.
   - 요청만으로 결정할 수 없는 제품 방향이나 서로 배타적인 선택이 필요한 경우
   - 요청 범위를 넘어서는 대규모 기능 확대가 필요한 경우
   - 사용자 데이터 삭제·초기화처럼 복구하기 어려운 변경이 필요한 경우
   - 이전 Youffice 폴더나 성공 백업을 수정·실행·삭제해야 하는 경우
   - 외부 유료 서비스, 계정 권한, 비밀정보 또는 별도 비용 승인이 필요한 경우
5. 사용자가 조사·설명·검토만 요청한 경우에는 수정 요청으로 확대 해석하지 않는다.

## 서버 실행 및 충돌 방지

1. `localhost:8501`로 화면을 확인하기 전, 해당 포트를 사용하는 프로세스와 실행 경로를 먼저 확인한다. HTTP 200 응답만으로 현재 작업본이 실행 중이라고 판단하지 않는다.
2. 8501 포트에는 현재 작업 폴더의 `.venv\Scripts\python.exe -m streamlit run app.py` 서버 한 개만 실행한다. 이전 복사본이나 전역 Python 서버가 남아 있으면, 사용자에게 알린 뒤 해당 서버만 종료하고 현재 작업본을 다시 시작한다.
3. UI 수정 검증은 임시 포트(예: 851x)에서 먼저 확인하고, 사용자가 실제 화면 확인을 요청했을 때만 8501의 현재 작업본 서버를 재시작한다.
4. 실행 파일과 작업 폴더 경로가 일치하는지 확인한다. 기존 `Youffice` 폴더나 백업 폴더는 현재 작업본이 정상 기동되는 것을 확인하기 전 삭제하거나 수정하지 않는다.
5. 서버 재시작 후에는 `/_stcore/health` 확인과 함께, 실행 경로 및 수정된 화면 요소를 확인한다. 문제가 보이면 새 업무를 실행하지 말고 서버 경로·프로세스부터 점검한다.

## 작업 대상 폴더 고정

1. 모든 코드 조사, 수정, 테스트, 앱 실행은 현재 작업 중인 YOUFFICE 프로젝트 루트 안의 파일만 대상으로 한다.
2. 이전 프로젝트 폴더와 성공 백업 폴더는 현재 작업 대상이 아니다. 해당 폴더의 파일을 수정·실행·삭제하기 전에는 사용자에게 이유와 범위를 알리고 확인을 받는다.
3. 파일 경로가 모호하거나 localhost 화면과 현재 작업본의 일치 여부를 확인할 수 없으면, 변경이나 새 업무 실행보다 먼저 작업 폴더·서버 프로세스·실행 경로를 점검한다.

## 검색 비용 절약

1. 넓은 코드 조사를 시작하기 전에 `CODEBASE_MAP.md`에서 담당 파일을 먼저 찾는다.
2. 일반 코드 검색은 기본적으로 `data/`, `.venv/`, 성공 백업,
   `assets/`, `static/`을 제외한다. 백업·환경·이미지 작업일 때만 해당 폴더를 연다.
3. `rg` 결과에 백업을 포함하지 않는다. 이전 버전 비교가 필요하면
   `data/codex_backups/` 안의 정확한 백업 폴더 하나만 지정한다.
4. 대형 파일 전체를 먼저 읽지 않는다. 함수명·문구를 `rg -n`으로 찾은 뒤 필요한
   줄 범위와 직접 import하는 모듈만 읽는다.

## AI별 담당 범위

### ChatGPT

담당:
- 전체 구조 설계
- 오류 원인 분석
- 통합 테스트
- 데이터베이스 상태 점검
- 백업 및 복구 절차
- app.py, database.py 통합 변경 검토

기본 수정 범위:
- tools/
- 프로젝트 루트의 문서 파일

제한:
- 다른 AI가 작업 중인 파일을 동시에 수정하지 않는다.
- app.py와 database.py는 백업 후 한 작업씩만 수정한다.

### Gemini Code Assist

담당:
- ui/
- workflow/
- animation/ 내부 Python 코드

제한:
- conversation/의 프롬프트를 수정하지 않는다.
- app.py 전체 리팩터링을 한 번에 수행하지 않는다.
- database.py 구조를 임의로 변경하지 않는다.

### Google AI Studio

담당:
- conversation/
- 직원별 시스템 프롬프트
- 응답 형식
- 추측 금지 규칙
- 검수 및 보고서 프롬프트 품질

제한:
- UI 및 업무 실행 코드를 수정하지 않는다.
- database.py와 workflow/를 수정하지 않는다.

### PixelLab

담당:
- animation/assets/
- 직원 캐릭터 이미지
- 동작별 애니메이션 이미지
- 배경 및 스프라이트 리소스

제한:
- Python 코드를 수정하지 않는다.
- 파일명과 폴더 구조를 임의로 바꾸지 않는다.

### v0

담당:
- UI 디자인 참고안
- 화면 배치와 컴포넌트 시안

제한:
- 실제 프로젝트 파일을 직접 수정하지 않는다.
- v0가 만든 코드를 그대로 app.py에 붙여넣지 않는다.

## 통합 핵심 파일

다음 파일은 담당 AI가 단독으로 수정하지 않는다.

- app.py
- database.py
- requirements.txt
- conversation/__init__.py
- workflow/__init__.py
- ui/__init__.py

수정 절차:

1. 변경 목적 정리
2. 성공 버전 백업 확인
3. 한 파일씩 수정
4. 문법 검사
5. import 검사
6. 앱 실행 검사
7. 새 백업 생성

## 현재 기준 성공 백업

Youffice_stage5_success_backup_20260807_0222

이 폴더는 수정하지 않는다.
